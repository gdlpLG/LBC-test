services:
  lbc-finder:
    build: .
    container_name: lbc-finder
    ports:
      - "5090:5000" # Port Host : Port Container (Modifier 5090 si utilisé)
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/leboncoin_ads.db
      - PORT=5000
    restart: unless-stopped
